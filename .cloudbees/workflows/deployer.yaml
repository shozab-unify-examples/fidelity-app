apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer
on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:
  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-api']['hackers-api'].deploy }}
    uses: TARGET_ORG/PROJECT_NAME-api/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-api']['hackers-api'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-api']['hackers-api'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-auth']['hackers-auth'].deploy }}
    uses: TARGET_ORG/PROJECT_NAME-auth/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-auth']['hackers-auth'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-auth']['hackers-auth'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-web']['hackers-web'].deploy }}
    uses: TARGET_ORG/PROJECT_NAME-web/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-web']['hackers-web'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['PROJECT_NAME-web']['hackers-web'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
