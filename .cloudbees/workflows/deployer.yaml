apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:

  # ============================================================
  # HACKERS API
  # ============================================================

  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].deploy }}

    steps:
      - name: Simulate hackers-api deployment
        kind: deploy
        run: |
          echo "========================================"
          echo "HACKERS API DEPLOYMENT"
          echo "========================================"
          echo "Component: hackers-api"
          echo "Environment: ${{ inputs.environment }}"
          echo "Artifact ID: ${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].id }}"
          echo "Artifact URL: ${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].url }}"
          echo
          echo "Starting deployment simulation..."
          sleep 5
          echo
          echo "Deployment simulation completed successfully."

      - name: Register hackers-api deployment
        uses: cloudbees-io/register-deployed-artifact@v2
        with:
          artifact-id: ${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].id }}
          target-environment: ${{ inputs.environment }}
          labels: demo,hackers-api

      - name: Publish hackers-api deployment evidence
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: |-
            # Deployment Registration Completed

            **Component:** `hackers-api`

            - **Environment:** `${{ inputs.environment }}`
            - **Artifact ID:** `${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].id }}`
            - **Artifact URL:** `${{ fromJSON(inputs.manifest)['fidelity-api']['hackers-api'].url }}`
            - **Deployment status:** `SUCCESS`
            - **Environment registration:** `COMPLETED`
            - **Deployment mode:** `SIMULATED`

            The artifact was successfully registered as deployed to the
            target CloudBees Unify environment.

            This demonstration records deployment activity and environment
            inventory without claiming that a live Kubernetes workload was started.
          format: MARKDOWN


  # ============================================================
  # HACKERS AUTH
  # ============================================================

  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].deploy }}

    steps:
      - name: Simulate hackers-auth deployment
        kind: deploy
        run: |
          echo "========================================"
          echo "HACKERS AUTH DEPLOYMENT"
          echo "========================================"
          echo "Component: hackers-auth"
          echo "Environment: ${{ inputs.environment }}"
          echo "Artifact ID: ${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].id }}"
          echo "Artifact URL: ${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].url }}"
          echo
          echo "Starting deployment simulation..."
          sleep 5
          echo
          echo "Deployment simulation completed successfully."

      - name: Register hackers-auth deployment
        uses: cloudbees-io/register-deployed-artifact@v2
        with:
          artifact-id: ${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].id }}
          target-environment: ${{ inputs.environment }}
          labels: demo,hackers-auth

      - name: Publish hackers-auth deployment evidence
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: |-
            # Deployment Registration Completed

            **Component:** `hackers-auth`

            - **Environment:** `${{ inputs.environment }}`
            - **Artifact ID:** `${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].id }}`
            - **Artifact URL:** `${{ fromJSON(inputs.manifest)['fidelity-auth']['hackers-auth'].url }}`
            - **Deployment status:** `SUCCESS`
            - **Environment registration:** `COMPLETED`
            - **Deployment mode:** `SIMULATED`

            The artifact was successfully registered as deployed to the
            target CloudBees Unify environment.

            This demonstration records deployment activity and environment
            inventory without claiming that a live Kubernetes workload was started.
          format: MARKDOWN


  # ============================================================
  # HACKERS WEB
  # ============================================================

  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].deploy }}

    steps:
      - name: Simulate hackers-web deployment
        kind: deploy
        run: |
          echo "========================================"
          echo "HACKERS WEB DEPLOYMENT"
          echo "========================================"
          echo "Component: hackers-web"
          echo "Environment: ${{ inputs.environment }}"
          echo "Artifact ID: ${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].id }}"
          echo "Artifact URL: ${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].url }}"
          echo
          echo "Starting deployment simulation..."
          sleep 5
          echo
          echo "Deployment simulation completed successfully."

      - name: Register hackers-web deployment
        uses: cloudbees-io/register-deployed-artifact@v2
        with:
          artifact-id: ${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].id }}
          target-environment: ${{ inputs.environment }}
          labels: demo,hackers-web

      - name: Publish hackers-web deployment evidence
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: |-
            # Deployment Registration Completed

            **Component:** `hackers-web`

            - **Environment:** `${{ inputs.environment }}`
            - **Artifact ID:** `${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].id }}`
            - **Artifact URL:** `${{ fromJSON(inputs.manifest)['fidelity-web']['hackers-web'].url }}`
            - **Deployment status:** `SUCCESS`
            - **Environment registration:** `COMPLETED`
            - **Deployment mode:** `SIMULATED`

            The artifact was successfully registered as deployed to the
            target CloudBees Unify environment.

            This demonstration records deployment activity and environment
            inventory without claiming that a live Kubernetes workload was started.
          format: MARKDOWN